**Project Overview**

recrudx is a small CRUD application built using React and Redux Toolkit.
The project demonstrates how to manage state efficiently through slices and centralized store handling.
TailwindCSS is used to style the interface with clean and responsive design utilities.
The goal is to provide a simple example of creating, updating, displaying, and deleting records while keeping the codebase organized and scalable.
